installer:
- DONE - close existing instances of seamly2d, seamlyme, and seamlylayout

seamly2d:
- DONE - When the user assigns a piece name to a pattern piece through the properties dialog, the piece name should be unique and its svg id should be updated.
- DONE - The <rect> element within each pattern piece should be named id="rect_<piecename>"

seamlylayout:
- DONE - Export to SVG incorrectly retains the "Rectangles" group <g id="Rectangles"> and the <rect> element withing rectangle within each pattern piece. 
- DONE - Fix: View's file picker doesn't highlight the selected file - it's unclear which file is being selected. It should highlight the file currently selected. 
- DONE - Fix: Adjust mode displays the piece overlays but not the pieces. 
- DONE - Settings > Layout options > Layout mode section --> add option "Any" where process_layout() does not care about grainline direction when placing the pattern pieces efficiently. 
- DONE - Adjust mode --> the menu with Fit, Zoom In, Zoom Out, etc should have white text, not black text so that the user can read it. 
- In adjust mode, the 'Lower to Bottom' operation isn't correct: When I move a large piece completely over a small piece, then select the large piece apply the 'lower to bottom' operation to it, then select the small piece and select any operation with the context menu, then the operation is applied to the large piece (incorrect) when it should be applied to the small piece (correct). Explain why then create a plan to fix this problem, discuss with me, if I approve then implement the plan. 