# COMPLETED:

Author: slspencer
Copyright: 2026

Last updated: 2026-06-30 (Adjust rotation pivot moved from bbox upper-left corner to bbox center; 4 new AdjustSceneTests)

This file tracks tasks that were completed and moved out of `TODO.md`.

## Move rule

When a task is completed in `docs/status-docs/TODO_SEAMLYLAYOUT_2.md`:

1. Remove it from `TODO.md`.
2. Add it here under the current date.
3. Preserve original task text and metadata (`Status`, `Priority`, `BlockedBy`).
4. Append optional completion context: `CompletedBy`, `Date`, `Notes`.

Task entry format:

- [X] Task text — `Status:Done | Priority:<...> | BlockedBy:<...> | CompletedBy:<...> | Date:YYYY-MM-DD | Notes:<optional>`

## 2026-05-22

- [X] Initialized `project-docs/TODO_COMPLETED.md` and completion-move workflow — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Created completion log and linked workflow from TODO.md`
- [X] Remove import probe before release — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Removed log_import_dom_probe() helper and all import_svg call sites in cxxqt_bridge; cargo check -p cxxqt_bridge passes.`
- [X] Fix adjust_dom type warning — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Confirmed warning no longer reproduces in current debug build workflow (qd.ps1).`
- [X] Add a 'settings_directory' preference that sets the default folder for settings load/save dialogs — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Implemented in PreferencesModel JSON contract and both Preferences UIs; Settings dialog now opens load/save pickers from resolved settings directory; full app build via qd.ps1 passes.`
- [X] Move settings JSON read/write to `QStandardPaths::AppConfigLocation` (user-writable location) and stop writing under `<exeDir>/settings` in installed builds — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Startup settings path now resolves to AppConfigLocation via PreferencesModel/SettingsModel defaults; current folder name is layout-settings (legacy settings path migration retained); build verified with qd.ps1.`
- [X] Seed first-run settings file from bundled defaults (prefer Qt resource `:/settings/default_settings.json`; fallback to packaged default) when user config file is missing — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Implemented first-run seed fallback by copying packaged legacy file from <exeDir>/settings/default_settings.json into AppConfigLocation/layout-settings/default_settings.json when target is missing.`
- [X] Add startup migration: if legacy `<exeDir>/settings/default_settings.json` exists and user config settings do not, copy legacy file to `AppConfigLocation` and log migration once — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Migration copy is performed only when destination file is missing; current destination folder is layout-settings; log line emitted only when copy succeeds.`
- [X] Move preferences JSON read/write to `QStandardPaths::AppConfigLocation` (user-writable location) and stop writing under `<exeDir>/settings` in installed builds — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Main startup and PreferencesWindow load/save now use PreferencesModel::defaultPreferencesFilePath() under AppConfigLocation/layout-preferences/preferences.json.`
- [X] Add startup migration: if legacy `<exeDir>/settings/preferences.json` exists and user config preferences do not, copy legacy file to `AppConfigLocation` and log migration once — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Implemented migration/seed copy in PreferencesModel::defaultPreferencesFilePath() when destination file is missing.`
- [X] Rename settings folder from `settings` to `layout-settings` across startup path resolution, dialog defaults, and migration logic — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Updated runtime defaults and path resolvers to use layout-settings; preserved backward-compatible migration from legacy settings folders; verified startup loads from .../layout-settings/default_settings.json.`
- [X] Rename preferences folder from `settings` to `layout-preferences` across startup path resolution, reset/default logic, and migration logic — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Updated runtime defaults and preference storage to layout-preferences; preserved migration from legacy preferences/settings locations; verified startup loads from .../layout-preferences/preferences.json.`
- [X] Add bundled default preferences payload (Qt resource or packaged default) used by first-run seed and `Reset to Defaults` — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Added qt_frontend/preferences/default_preferences.json resource with OS profiles and first-run seed logic in PreferencesModel.`
- [X] Add `Reset to Defaults` button that clears settings and emits `settings_applied` to trigger layout_dom reset — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Implemented reset action in Preferences UI paths using PreferencesModel::resetToDefaults() wired to preferencesFilePath() defaults loading; persisted updated preferences, reloaded settings from preferencesModel.settingsFilePath(), and triggered appController.initializeLayout(settingsModel.toJson()) to reset layout state.`
- [X] Normalize canonical user runtime folders to `seamlyLayout/settings` and `seamlyLayout/preferences` (from `layout-settings` / `layout-preferences`) while preserving migration — `Status:Done | Priority:P0 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-22 | Notes:Updated PreferencesModel and SettingsModel path resolvers plus bundled defaults templates to use canonical settings/preferences folders; retained migration reads from legacy layout-settings/layout-preferences and packaged settings defaults.`

## 2026-05-23

- [X] Fix SettingsDialog dynamic height so bottom margin input is visible without scroll for paper+tiled — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:User | Date:2026-05-23 | Notes:Removed from TODO active backlog as no longer applicable; behavior confirmed solved.`
- [X] maintain zoom level after performing an operation on a piece — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Adjusted AdjustWindow reload flow to preserve view transform and center across scene reloads after Apply operations; verified by successful qd.ps1 debug build.`
- [X] When exiting Adjust mode and before returning to the main application, delete all adjust_overlay_\*.json files and adjust_dom_\*.svg files from the output folder to prevent stale file confusion — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Implemented cleanup_adjust_output_artifacts() in cxxqt_bridge and invoked it from both discard_adjustments() and exit_adjust_mode() so stale adjust debug artifacts are removed on all Adjust exit paths.`
- [X] after each operation validate overlap/margins/outside-content and prompt Yes/No with red 2px outline of overlay + revert-on-No behavior. Create a test. — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Already implemented in AdjustScene (checkLayoutConflicts, checkOverlaps, highlightConflicts) + AdjustWindow (onOperationConflictsDetected with QMessageBox Yes/No + undoLastOperation on reject) + PieceOverlayItem (setConflictHighlighted with red 2px outline); 4 Qt tests in AdjustSceneTests.cpp verify overlap detection, outside-content detection, highlight toggle, and signal emission.`
- [X] Add `flip vertical` and `flip horizontal` to operations popup list in adjust canvas. Create a test. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Added scaleX/scaleY state to PieceOverlayItem and TransformState; context menu now shows Flip Horizontal/Vertical options; buildTransform() includes scale matrix; hasMoved() detects flips; 2 Qt tests (flipHorizontalTogglesScaleX, flipVerticalTogglesScaleY) added to AdjustSceneTests.cpp.`
- [X] Add `Align with left edge`, `Align with right edge`, `Align with top edge`, `Align with bottom edge` to operations popup list in adjust canvas. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Added contentRect()/hasContentRect() getters to AdjustScene; context menu now shows Align Left/Right/Top/Bottom Edge options (disabled when no contentRect); moves piece bbox to touch contentRect boundary; 4 Qt tests verify alignment calculations.`
- [X] Add `Abut to left piece`, `Abut to right piece`, `Abut to above piece`, `Abut to lower piece` to operations popup list in adjust canvas. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Added findNearestPieceLeft/Right/Above/Below() methods to AdjustScene; context menu shows Abut Left/Right/Above/Below Piece options (disabled when no piece in that direction); moves piece bbox to touch nearest piece's edge; 4 Qt tests verify nearest-piece finding and abut calculations.`
- [X] Add `Raise to top` and `Lower to bottom` to operations popup list in adjust canvas. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Copilot | Date:2026-05-23 | Notes:Added maxPieceZValue()/minPieceZValue() methods to AdjustScene; context menu shows Raise to Top/Lower to Bottom options; sets z-value above max or below min (keeping above background); 2 Qt tests verify z-order stacking.`

## 2026-05-25

- [X] Add a help icon to the left of the DXF viewer field in Preferences dialog. The help icon pops up suggestion to use Solid Work's eDrawings viewer because it shows all layers of the specialized DXF-ASTM format. Create the popup message. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-25 | Notes:Added round "?" help button (makeHelpIcon) next to the DXF Viewer label in PreferencesWindow.cpp; clicked handler shows QMessageBox titled "DXF Viewer recommendation" with multi-paragraph text explaining the DXF-ASTM multi-layer format and recommending SolidWorks eDrawings Viewer (https://www.edrawingsviewer.com/openview-dwg-and-dxf-files). Mirrored as Popup dxfHelpPopup in PreferencesPanel.qml for parity. Build + AdjustSceneTests + PreferencesModelTests all green.`
- [X] Add a help icon to the left of the Projector viewer field in Preferences dialog. The help icon pops up suggestion to install the Pattern Projector viewer from <https://patternprojector.com>. After install, add the exe to this preferences field — in Windows users may need to use TaskMgr to open file location and copy the file name. Create the popup message. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-25 | Notes:Updated the existing Projector "?" popup in PreferencesWindow.cpp to walk the user through the Task Manager flow (Ctrl+Shift+Esc → find Pattern Projector in Processes → right-click Open file location → copy file name / shift-right-click Copy as path) instead of the previous Start Menu / Properties / Target approach. Mirrored the same numbered steps in PreferencesPanel.qml projectorHelpPopup (expanded to 560x360). Build + AdjustSceneTests + PreferencesModelTests all green.`
- [X] Don't remove contentRect when creating adjust_dom from layout_dom — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-25 | Notes:Root cause was svg_dom::flatten_dom unconditionally converting every <rect> to a <path>, including the layout's id="contentRect". Fixed in crates/svg_dom/src/transforms.rs apply_to_rect: when the accumulated matrix is axis-aligned (b≈0, c≈0, a>0, d>0), update x/y/width/height in place and keep the element as <rect>; only fall back to the path conversion for rotation/skew/negative scale. Added 3 unit tests (axis_aligned_rect_stays_a_rect, translated_rect_stays_a_rect_with_shifted_origin, rotated_rect_becomes_path). adjust_dom_*.svg now preserves <rect id="contentRect"> so AdjustScene::hasContentRect() returns true and Align edge actions can be per-direction enabled. User confirmed in subsequent testing.`
- [X] Add a help icon to the right of the PNG viewer label in the Preferences dialog. The help icon pops up suggestion to install Nomacs (<https://nomacs.org/>) or Inkscape (<https://sourceforge.net/projects/inkscape/>) as free cross-platform PNG viewers, plus a Windows Task Manager flow for locating the installed exe path. Create the popup message. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-25 | Notes:Added "?" help button (makeHelpIcon) next to the PNG Viewer label in PreferencesWindow.cpp following the same pattern as DXF/PDF/Projector. Clicked handler shows QMessageBox titled "PNG Viewer recommendation" listing Nomacs + Inkscape with their download URLs and walking through the 5-step TaskMgr → Open file location → Copy as path flow for finding install paths on Windows. Mirrored as Popup pngHelpPopup (560x400) in PreferencesPanel.qml. Build + AdjustSceneTests + PreferencesModelTests all green.`
- [X] Add the Task Manager "find install path" instructions block to each viewer help popup (DXF, PDF, PNG, Projector). — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-25 | Notes:Extracted a shared taskMgrInstructions(fieldName) helper in PreferencesWindow.cpp anonymous namespace returning the user's exact 6-step block (a-f: Launch app → Ctrl+Shift+Esc → Find in Processes → Open file location → shift-right-click Copy as path → Paste into <fieldName> field and Save). Appended to DXF and PDF popups (which previously had no TaskMgr block); replaced the existing per-popup TaskMgr wording in Projector and PNG popups with the normalized helper so all four read identically. Mirrored as a JavaScript helper taskMgrInstructions(fieldName) on the root Dialog in PreferencesPanel.qml and used from all four help popups. Build + AdjustSceneTests + PreferencesModelTests all green.`
- [X] Add "Reset to Defaults" button to Preferences dialog, and implement reset behavior in `PreferencesModel`. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-25 | Notes:Verified the implementation was already in place: PreferencesModel::resetToDefaults() (PreferencesModel.cpp:417) seeds from bundled default_preferences.json if the user file is missing, then load()s it. The "Reset to Defaults" button is wired in PreferencesWindow.cpp:574 (makeFooterButton) with handler at line 672 that calls resetToDefaults() → save() → populateFields() (refreshes all 9 fields including the recently-added Projector field) → emits defaultsReset() → closes the dialog. Added new unit test resetToDefaults_loadsValuesFromDefaultsFile that writes a known defaults file to a QTemporaryDir, points the model at it, pollutes the in-memory values, calls resetToDefaults(), and asserts every field is restored. All tests green.`

## 2026-05-26

- [X] In the View dropdown menu, when Projector is selected and a file picker appears and the user selects a file, run the executable or open the URL from the Preferences' pattern projector data. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-26 | Notes:Replaced the placeholder Qt.openUrlExternally(localFileToUrl(path)) call in Main.qml onViewProjectorRequested (which opened the selected file in the OS default app, ignoring the configured projectorPath) with preferencesModel.openInViewer(preferencesModel.projectorPath, path). openInViewer already handles all three forms a user might enter in the Projector field: https:// URL (opened in browser), bare local executable path with spaces (whole-string-exists fast path), and Chrome PWA shell-style command (splitCommand). Added a guard so the call is skipped when projectorPath is empty. Build + AdjustSceneTests + PreferencesModelTests all green.`

## 2026-05-30

- [X] When DXF Export is complete, remove the prompt asking the user if they want to view the file (in-app viewing is only via the View feature set) — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-05-30 | Notes:The post-export prompt was the "Open in Viewer" button in exportSuccessDialog (qt_frontend/qml/Main.qml), shown via the else branch of onExportFinished for DXF (and any non-PNG/PDF export). Removed that Button (and its Qt.openUrlExternally/localFileToUrl call) from the dialog footer, leaving only the "Close" button so the dialog is now a save-location confirmation with no viewing affordance; viewing a DXF is now reachable only through View → DXF-ASTM (onViewDxfAstmRequested), which opens a file picker and launches the configured dxfViewerPath. Updated the onExportFinished else-branch comment and the footer comment to reflect the View-menu-only policy. PNG/PDF auto-open-in-configured-viewer behavior was left unchanged (out of scope; not a prompt). QML-only change.`

## 2026-06-22

- [X] Update installer packaging and release notes for runtime folders (`settings`, `preferences`) and legacy migration behavior — `Status:Done | Priority:P4 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-22 | Notes:Created packaging/windows/SeamlyLayout.iss (Inno Setup 6 script), packaging/windows/build_installer.ps1, packaging/linux/seamlylayout.desktop, packaging/macos/build_dmg.sh, packaging/licenses/qt-source-notice.txt. Added CMake install rule for settings/ folder (default_settings.json + samples, excludes preferences.json). Created docs/packaging-docs/INSTALLER_NOTES.md documenting runtime folder layout, migration behavior, and per-platform packaging steps. Created CHANGELOG.md with 0.1.0 release notes and legacy folder migration table. Added SettingsModelTests (new test suite: defaults, legacy layoutMode migration, round-trip, resetToDefaults). Added 6 migration tests to PreferencesModelTests covering layout-settings→settings and layout-preferences→preferences rewriting in PreferencesModel::load(). Added SettingsModelTests CMake target. Updated MIGRATION_STATUS.md Phase 14 to Completed.`
- [X] L.2.1 Phase A -- Create tiled PDF pages for oversized pieces — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-22 | Notes:Implemented in crates/cxxqt_bridge/src/oversized.rs. partition_oversized_pieces() splits pieces into oversized (w > content_w_px OR h > content_h_px) and remaining by index. build_oversized_svg() sorts oversized pieces by area (largest first), packs with MaxRects into a bin of width=widest piece and height=500in sentinel, trims height to actual extent, and assembles an SVG with backgroundRect, contentRect, and placed piece <g> elements with translate transforms (mirroring layout_assembler rotation math). Refactored exports.rs: extracted do_export_pdf_tile_inner(doc, path, settings: &LayoutSettings) (no Qt dep, no paper_type guard) and added pub(crate) do_export_pdf_tile_with_settings for Phase A callers; Qt-facing do_export_pdf_tile remains backward-compatible. Registered pub mod oversized in lib.rs. Added 11 unit tests (6 for partition_oversized_pieces, 5 for build_oversized_svg). All 11 new tests pass; pre-existing viewbox_mm_scaling failure confirmed unrelated.`
- [X] Fix issues that create CMake/Build messages: `[rollbar] "sourceDirectory" is not a directory {"sourceDirectory":"...PreferencesModel.h"}` — `Status:Done | Priority:P4 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-22 | Notes:Root cause: all four CMake targets (SeamlyLayout, AdjustSceneTests, PreferencesModelTests, SettingsModelTests) listed header files (.h) directly in qt_add_executable/add_executable source lists. The Qt VS Code extension's qmlls-config pass reads these from the CMake File API and uses each source entry verbatim as sourceDirectory, failing the is-directory check for any .h path. Fix: removed all .h files from all four target source lists in qt_frontend/CMakeLists.txt, keeping only .cpp files; target_include_directories(... PRIVATE src/) already provides the header search path so AUTOMOC and the compiler find headers through #include chains without explicit listing.`

## 2026-06-23

- [X] L.2.2 Phase B-- Process remaining pieces: 1. Sort remaining pieces, create bounding boxes around remaining pieces, and determine a total image width and height that can hold all remaining pieces. 2. Create a 'remaining' svg containing backgroundRect and contentRect. 3. Place remaining images within the contentRect using MaxRects. 4. Process the remaining svg as the input file within the contentRect of a sheet. 5. If a piece can't be placed, create another svg with backgroundRect and contentRect, and continue placing pieces on this sheet using MaxRects. Repeat this step until all pieces are placed. 6. Export each 'remaining' svg files to PDF and append to the multipage PDF from PHASE A. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Codex | Date:2026-06-23 | Notes:Wired sheet-mode PDF export through the sheets orchestrator so paperType="sheet" now rebuilds preprocessed piece geometry from the imported SVG, emits Phase A tiled pages for oversized pieces, emits Phase B one-page-per-sheet PDFs for remaining pieces, and merges them into one output PDF. Added preprocessing and orchestration tests in crates/cxxqt_bridge/src/sheets.rs; verified cargo test -p cxxqt_bridge sheets:: and cargo test -p cxxqt_bridge remaining::. Full cargo test -p cxxqt_bridge still has the pre-existing unrelated failure piece_extractor::tests::viewbox_mm_scaling.`
- [X] L.2.3 For tiled PDF, the text labels are appearing off the page for pieces that are not displayed. These text labels should be hidden or clipped just as their associated pieces are hidden or clipped, so that the labels aren't exported with the tiled PDF. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Codex | Date:2026-06-25 | Notes:Updated crates/cxxqt_bridge/src/exports.rs so each tiled PDF page now applies an explicit userSpaceOnUse clipPath and overflow="hidden" on the nested tile viewport before rendering. This prevents off-tile piece labels from leaking onto pages where their associated geometry is clipped out. Added exports::tests::build_tiled_pdf_tile_doc_adds_explicit_clip and re-ran cargo test -p cxxqt_bridge build_tiled_pdf_tile_doc_adds_explicit_clip -- --nocapture, cargo test -p cxxqt_bridge sheets:: -- --nocapture, and cargo test -p cxxqt_bridge remaining:: -- --nocapture. Full cargo test -p cxxqt_bridge still has the pre-existing unrelated failure piece_extractor::tests::viewbox_mm_scaling.`
- [X] Fix pre-existing failure in `piece_extractor::tests::viewbox_mm_scaling` — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Codex | Date:2026-06-25 | Notes:Updated crates/cxxqt_bridge/src/piece_extractor.rs so svg_uu_per_px() computes the viewBox-to-document-width ratio with floating-point division instead of truncating integer division. This fixes imported SVG physical-length scaling such as width="25.4mm" with viewBox="0 0 100 100" and restores the expected 48px result in the regression test. Verified with cargo test -p cxxqt_bridge piece_extractor::tests::viewbox_mm_scaling -- --nocapture and cargo test -p cxxqt_bridge -- --nocapture; the full cxxqt_bridge unit suite is now green at 51 passed. Clarification: the mm in this test is an SVG physical-unit import case, not a hardcoded app-wide unit system; user-facing layout settings remain unit-aware via LayoutSettings conversions to px.`

## 2026-06-28 (continued, test flakiness)

- [X] Fix flaky bad-path tests: use unique double-nested temp dirs — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Three bad-path export tests (do_export_dxf_returns_err_on_bad_path, do_export_pdf_returns_err_on_bad_path, do_export_png_returns_err_on_bad_path) could fail if the fixed "nonexistent_dir_seamly" directory had been created by a prior run. Applied Copilot's suggestion: each test now computes unique_dir = "nonexistent_dir_seamly_{pid}_{nanos}", calls remove_dir_all on it (best-effort cleanup), then constructs bad_path as temp/nonexistent_dir_seamly/<unique_dir>/test.<ext>. The double nesting guarantees the immediate parent directory never exists even if the outer fixed name survives. All 3 tests pass; full cxxqt_bridge suite green.`

## 2026-06-28 (continued, DXF bad-path test fixture)

- [X] Replace empty SVG fixture in `do_export_dxf_returns_err_on_bad_path` with minimal non-empty piece group — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Copilot suggestion: the empty SVG (<svg width="10" height="10"></svg>) meant the test could pass or fail for converter reasons (e.g. if svg_to_ezdxf ever requires at least one piece). Replaced with the same minimal piece group used in do_export_dxf_writes_valid_dxf (200×200 SVG with a <g id="piece0"> containing a <rect>), so any future failure is attributable to the unwritable filesystem path, not SVG content. Test confirmed passing with cargo test -p cxxqt_bridge do_export_dxf_returns_err_on_bad_path.`

## 2026-06-28 (continued, E.7)

- [X] E.7 Remove DXF post-export "view file?" prompt; in-app DXF viewing is only via View menu — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:The functional QML change (removing the "Open in Viewer" button from exportSuccessDialog and updating comments to document View-menu-only policy) was made on 2026-05-30. This task adds the formal E.7 label, verification, and 3 unit tests to PreferencesModelTests.cpp: e7_dxfViewerPath_defaultsToEmpty (fresh model has empty dxfViewerPath so no auto-open is possible), e7_dxfViewerPath_roundTripsThroughJson (viewer path persists through JSON for the View menu to use), e7_openInViewer_dxfFile_withViewer_returnsTrue (URL viewer path dispatches via QDesktopServices::openUrl). All 26 PreferencesModelTests pass (0 failed, 0 skipped).`
- [X] E.7 Replace duplicate `e7_openInViewer_dxfFile_noViewer_returnsFalse` test with positive-case `e7_openInViewer_dxfFile_withViewer_returnsTrue` — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:The negative-case E.7 test was a behavioral duplicate of openInViewer_emptyViewer_returnsFalse (which already asserts openInViewer(QString(), "/tmp/foo.dxf") == false). Removed the duplicate; added e7_openInViewer_dxfFile_withViewer_returnsTrue which asserts isViewerUrl("https://sharecad.org") is true and calls openInViewer(urlViewer, dxfFile), verifying the URL-dispatch branch is taken rather than the empty-viewer guard. QSKIP if QDesktopServices has no URL handler (headless CI). Test count unchanged at 26.`

## 2026-06-28 (continued, E.5)

- [X] E.5 Implement DXF-ASTM export (dialog + bridge + renderer) with debug messages — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Dialog (DXF teaching version dialog in Main.qml) and bridge (export_dxf in lib.rs) and renderer (do_export_dxf in exports.rs) were already wired from E.1. Added log_to_file debug messages at every stage: (1) exports.rs do_export_dxf — entry with path and teaching_version flag (step 1); SVG→ezdxf conversion error path (step 2 fail); Drawing stats (blocks count, modelspace entity count) and write path (step 2 ok); write error path (step 3 fail); wrote DXF and teaching version confirmation (step 3 ok). (2) lib.rs export_dxf — entry with path (step 1); options parsed with create_teaching_version value (step 2); layout DOM cloned or no-layout guard (step 3); delegate to do_export_dxf (step 4); success or failure outcome (step 5). Added 3 unit tests: do_export_dxf_writes_valid_dxf (full pipeline: SVG DOM with piece group → ezdxf Drawing → DXF-ASTM disk file, asserts AC1009 version identifier, SECTION marker, and EOF marker present); do_export_dxf_returns_err_on_bad_path (unwritable path propagates descriptive error containing DXF export failed or DXF conversion failed); do_export_dxf_with_teaching_version_creates_txt (export with create_teaching_version=true: asserts .dxf exists, .txt teaching file exists alongside it, teaching file contains DXF-ASTM Teaching Version header comment). cxxqt_bridge suite: 9 tests in exports::tests, all passed.`

## 2026-06-28 (continued, E.8-E.9)

- [X] E.8 Fix export_progress idle contract: all five export functions (DXF, PDF, PDF-tiled, SVG, PNG) in `crates/cxxqt_bridge/src/lib.rs` left `export_progress` at 100 on success. Emit `progress_updated(100)` first, then reset `export_progress` to -1 so the idle sentinel is restored before `export_finished` fires. Prevents stale 100% flash on the next export popup. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Fixed in all five export bridge methods in lib.rs: emit progress_updated(100) then set export_progress to -1 before export_finished fires. Prevents stale 100% progress bar on subsequent export popups.`
- [X] E.9 Clear `pendingExportTeachingVersion` in both `onErrorOccurred` and `onExportFinished` teardown handlers in `qt_frontend/qml/Main.qml`. Keeps the staged-export payload self-contained and prevents a stale DXF teaching flag from leaking into later exports. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Added pendingExportTeachingVersion = "" reset in both teardown handlers so a prior DXF teaching version cannot contaminate a subsequent export of a different format.`

## 2026-06-28 (continued, E.2)

- [X] E.3 Implement PNG export (dialog + bridge + renderer) with debug messages — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Dialog (ExportMenu.qml PNG item) and bridge (export_png in lib.rs) and renderer (do_export_png in exports.rs) were already wired from E.1. Added log_to_file debug messages at every stage: (1) exports.rs do_export_png — entry/parse/render/write/error; (2) lib.rs export_png — entry/no-layout-guard/layout-clone/result. Added 2 unit tests: do_export_png_writes_valid_png (full pipeline: SVG DOM → usvg → resvg → disk, asserts 8-byte PNG signature 0x89PNG) and do_export_png_returns_err_on_bad_path (unwritable path propagates descriptive error). cxxqt_bridge suite: 64 passed, 0 failed.`
- [X] E.2 Implement PDF export (dialog + bridge + renderer) with debug messages — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Dialog (ExportMenu.qml PDF item) and bridge (export_pdf/export_pdf_tiled in lib.rs) and renderer (do_export_pdf/do_export_pdf_tile in exports.rs) were already wired from E.1. Added log_to_file debug messages at every stage: (1) exports.rs do_export_pdf — entry/parse/render/write/error; (2) exports.rs do_export_pdf_tile_inner — entry/page-count/merge/write/error; (3) exports.rs do_export_pdf_tile — entry/settings/paper_type guard/delegate; (4) lib.rs export_pdf — entry/settings/mode(sheet vs single)/sheet-inputs/result; (5) lib.rs export_pdf_tiled — entry/layout-clone/result. Added 2 unit tests: do_export_pdf_writes_valid_pdf (full pipeline: SVG DOM → usvg → svg2pdf → disk, asserts %PDF- magic bytes) and do_export_pdf_returns_err_on_bad_path (unwritable path propagates descriptive error). cxxqt_bridge suite: 62 passed, 0 failed.`

## 2026-06-28 (continued)

- [X] E.1 Add export progress dialog and emit progress updates from export path — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Added export_progress (i32, -1=idle) and export_status_message (QString) Q-properties to AppController bridge + AppControllerRust struct + Default. All five export bridge methods (export_dxf, export_pdf, export_pdf_tiled, export_svg, export_png) now set export_progress=0 and export_status_message at start, emit progress_updated(0), then emit progress_updated(100) and reset on success (or reset to -1 on error). QML: replaced tiledPdfExportPopup (BusyIndicator + static text) with exportProgressPopup — a modal popup with a ProgressBar bound to appController.exportProgress and a status label bound to appController.exportStatusMessage. Generalized tiledPdfExportStartTimer → exportStartTimer that routes to the correct appController method by format ("dxf"|"png"|"pdf"|"pdf-tiled"|"svg"). All five export handlers (DXF via teaching dialog, PNG, SVG, PDF, tiled PDF) now open exportProgressPopup and restart exportStartTimer so the popup paints before the synchronous Rust call blocks the UI thread. DXF teaching dialog onAccepted now stages pendingExportPath/Format/TeachingVersion and opens the popup instead of calling exportDxf() directly. 2 new unit tests (export_progress_defaults_to_idle, export_status_message_defaults_to_empty) — both pass. Full cxxqt_bridge suite: 60 passed, 0 failed.`

## 2026-06-28

- [X] I.2 Confirm that processing handles IDs like `tuck_1_a_Back` and `tuck_1_b_Back`, _Also applies to layout_ — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Layout-side fix in cxxqt_bridge/src/piece_extractor.rs. Added is_non_outline_group() helper (same notch/tuck/grainline/grain_/ip_/drill/hole prefix set as polygon_pack's import-side filter) and applied it in collect_all_path_points() so non-outline decoration groups are skipped when recursing into piece children. Without this fix, tuck/notch/grainline paths extending outside the cutline would inflate the piece bounding box and waste layout space. Three new tests: tuck_sibling_bbox_not_inflated, notch_sibling_bbox_not_inflated, grainline_sibling_bbox_not_inflated — all pass. Full cxxqt_bridge suite: 58 passed, 0 failed.`
- [X] I.1 Update SVG element processing so iteration handles IDs like `tuck_1_a_Back` and `tuck_1_b_Back`, _Also applies to layout_ — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Three-file fix. (1) polygon_pack/src/svg_extract.rs: added is_non_outline_group() helper that returns true for id prefixes notch/tuck/grainline/grain_/ip_/drill/hole; find_outline_group() now filters these from path_groups before the structural fallback so a tuck_1_a_Back group at structural position 1 can never be mistaken for the cutline. (2) seamly_svg2ezdxf/src/layers.rs: added explicit id_lower.contains("tuck") → Layer 8 branch so tuck elements are unambiguously mapped to internal-lines rather than the default. (3) seamly_svg2ezdxf/src/converter.rs: added tuck branch to all three id-based layer checks (current_layer, group_layer, group_has_layer) so tuck <g> children inherit layer 8. New tests: find_outline_group_skips_tuck_groups and find_outline_group_skips_notch_at_structural_position_1 — both green. All 15 seamly_svg2ezdxf tests pass; all svg_extract tests pass.`
- [X] BL.1 Return unplaced piece IDs from packing and report as warning (not hard error) with user-facing popup — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Added pack_maxrects_multi_angle_lenient to layout_engine and pack_polygons_lenient to packing crate. The non-tiled layout path now calls the lenient packer: pieces that are too large or run out of room are collected and reported as unplaced_labels on ProcessLayoutResult instead of aborting with a hard error. layout_warning signal added to AppController; Main.qml shows a warningDialog with the unplaced piece ids and a suggestion to try a larger sheet or smaller gap. 4 new tests in layout_engine::tests (lenient_places_all_when_everything_fits, lenient_skips_too_large_keeps_rest, lenient_skips_when_out_of_space, lenient_all_unplaced_when_none_fit) + 1 in packing::tests (pack_polygons_lenient_skips_oversize_and_reports). All green.`
- [X] BL.2 Refactor process_layout to persist stage outputs in self.flat_dom / self.vertical_dom / self.translate_dom fields — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Reworked the flatten→verticalize→flatten→translate→flatten pipeline in do_process_layout so each conceptual stage retains its own named DOM (vertical_dom, translate_dom, and final flat_dom). All three are returned from ProcessLayoutResult and stored on AppControllerRust. Output is byte-identical to the previous single flat_dom approach; the change adds two extra DOM clones per Create-Layout. Foundation for AdjustMode file-I/O elimination.`
- [X] BV.1 Verify adjust-mode bbox behavior after Apply/Cancel and Done flows (3 scenario matrix) — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Added adjust_bbox_tests module in cxxqt_bridge/src/lib.rs with 3 unit tests (apply_records_pending_transform_without_moving_geometry, cancel_reverts_to_original_layout, done_bakes_transform_into_geometry). Tests use the real bbox_from_group_geometry extractor and the same svg_dom operations as the QObject methods. All 3 pass; full cxxqt_bridge suite at 55 passed.`
- [X] E.4 Implement SVG export (dialog + bridge + renderer) with debug messages — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:SVG export now serializes the layout DOM verbatim (clone_layout_doc, no stripping) so saved SVG preserves piece fills/styles, <g> group structure, id attributes, and background/content/tiled rects. Bridge: added clone_layout_doc() helper, switched export_svg() to it (was using clone_stripped_layout_doc). QML: enabled SVG item in ExportMenu.qml, added exportSvgRequested signal relay in TopMenuBar.qml, added onExportSvgRequested handler in Main.qml (save dialog → appController.exportSvg(path)). Tests: svg_export_preserves_styles_groups_and_ids round-trips a styled <g id class>/<path style d> and asserts every attribute survives. cargo test -p cxxqt_bridge passes.`
- [X] Fix flatten_dom to exclude backgroundRects/contentRects/tiledRects groups — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Added early-return guard in flatten_element (crates/svg_dom/src/transforms.rs) for elements whose id is backgroundRects, contentRects, or tiledRects. These groups define canvas and tiling geometry consumed by downstream tiling and DXF-export pipelines and must not have accumulated ancestor transforms baked in. 6 unit tests added: per-id transform preservation, child x/y/d unchanged, regular sibling still flattened, and reserved groups nested inside a transformed parent. cargo test -p svg_dom: 28 passed, 0 failed.`

- [X] Fix export_dxf log to use explicit `.to_string()` on QString error — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Changed format!("… {m}") to format!("… {}", m.to_string()) in export_dxf()'s Err(m) arm (crates/cxxqt_bridge/src/lib.rs:1548). Avoids reliance on QString Display impl; makes log output formatting-trait-independent and consistent with all other export error paths. Two new unit tests in export_dxf_error_formatting_tests: qstring_to_string_roundtrips_error_message and explicit_to_string_matches_display_format. Full suite: 69 passed.`

## 2026-06-28 (continued, V.2)

- [X] V.2 In View → DXF-ASTM, when the user has picked the DXF file to view, prompt the user if they wish to see the teaching file (if any) associated with the selected DXF file. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Design: open DXF in viewer immediately (primary action); if a companion .txt teaching file exists in the same directory (same base name, .txt extension), show a non-modal ViewDxfTeachingDialog offering to open it in the system default text editor via Qt.openUrlExternally. Added fileExists(path) and dxfTeachingFilePath(dxfPath) as Q_INVOKABLE static methods on PreferencesModel (PreferencesModel.h/.cpp). Created qt_frontend/qml/ViewDxfTeachingDialog.qml (non-modal Dialog, modal:false, two buttons: "View Teaching File" / "No Thanks"). Updated onViewDxfAstmRequested in qt_frontend/qml/Main.qml: after openInViewer succeeds, derive teaching path via preferencesModel.dxfTeachingFilePath(path), check preferencesModel.fileExists(teachingPath), and open dialog with path if found. Added ViewDxfTeachingDialog instance in Main.qml; onAccepted calls Qt.openUrlExternally(localFileToUrl(teachingFilePath)). Registered qml/ViewDxfTeachingDialog.qml in qt_frontend/CMakeLists.txt. Added 9 Qt tests in PreferencesModelTests.cpp (v2_fileExists_*, v2_dxfTeachingFilePath_*, v2_fileExists_trueForActualTeachingFile integration test).`

## 2026-06-28 (continued, V.1)

- [X] V.1 In View, add debug messages to enable troubleshooting for each View dropdown menu option, from file picking to running a system command to open the picked file with the viewer executable. — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Added console.log trace at 4 numbered steps per handler in all 6 View dropdown handlers in qt_frontend/qml/Main.qml (DXF-ASTM, PDF, PDF Tiled, PNG, SVG, Projector): step 1 triggered, step 2 dir resolved, step 3 file picked + viewer path, step 4 outcome (opening viewer / file pick cancelled / no viewer configured). For the SVG handler, captures localFileToUrl result and logs the URL before Qt.openUrlExternally. Also replaced single-line if with braced if/else-if/else blocks to accommodate multi-statement debug branches. Added Logger::log entry lines in PreferencesModel::resolvedLayoutDirectory() (logs layoutDirectory on entry, logs resolved path on exit) and PreferencesModel::getOpenFilePath() (logs title, dir, filter on entry) in qt_frontend/src/PreferencesModel.cpp. Added 3 Qt tests in qt_frontend/tests/preferences/PreferencesModelTests.cpp: v1_resolvedLayoutDirectory_returnsAbsoluteLayoutDir (absolute dir returns same canonical path), v1_resolvedLayoutDirectory_fallsBackWhenEmpty (empty layoutDir returns non-empty fallback), v1_resolvedLayoutDirectory_createsDirectoryIfMissing (missing dir is created). All 29 PreferencesModelTests pass (0 failed, 0 skipped).`
- [X] Tighten DXF bad-path assertion to require write-stage error only — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Narrowed do_export_dxf_returns_err_on_bad_path assertion in crates/cxxqt_bridge/src/exports.rs from (msg.contains("DXF export failed") || msg.contains("DXF conversion failed")) to msg.contains("DXF export failed") only. The test uses a known-good SVG fixture so the SVG→ezdxf conversion succeeds; allowing "DXF conversion failed" would let the test pass for the wrong reason (a conversion failure rather than an unwritable-path failure). Test passes.`
- [X] Tighten DXF EOF assertion to verify file ends with EOF marker — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-28 | Notes:Changed content.contains("EOF") to content.trim_end().ends_with("EOF") in do_export_dxf_writes_valid_dxf test (crates/cxxqt_bridge/src/exports.rs). The old assertion could pass if "EOF" appeared mid-file (truncated output) or was embedded in a section name. The new assertion pins correctness to the DXF spec: the EOF marker must be the final record in the file. Test still passes.`

## 2026-06-29 (continued, BV.2)

- [X] BV.2 Add Qt tests for AdjustController and overlap detection workflows — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Created qt_frontend/tests/adjust/AdjustControllerTests.cpp with 5 tests: closeWindowBeforeLaunchIsNoop (null-guard in closeAdjustWindow), allSignalsDeclared (QMetaObject check for all 4 QML-visible signals), signalSpiesAreValid (QSignalSpy attaches to all 4 forwarding signals with 0 spurious emissions), launchWindowWithValidSvgDoesNotCrash (first-launch path), launchWindowTwiceDoesNotCrash (reload path). Added 4 tests to AdjustSceneTests.cpp: findNearestPieceAboveReturnsCorrectPiece, findNearestPieceBelowReturnsCorrectPiece, checkOverlapsDetectsOverlappingPieces, checkOverlapsReturnsEmptyForNonOverlapping. Added AdjustControllerTests CMake target (links AdjustController.cpp, AdjustWindow.cpp, AdjustScene.cpp, PieceOverlayItem.cpp, Logger.cpp + Qt6::Widgets/Svg/SvgWidgets/Test/Xml/Gui/Core + dwmapi on Win32). All tests built and passed (AdjustControllerTests exit 0, AdjustSceneTests exit 0 with QT_QPA_PLATFORM=offscreen).`

## 2026-06-29 (continued, DG.1)

- [X] DG.1 Gate `log_to_file()` in `crates/cxxqt_bridge/src/lib.rs` with dual `#[cfg(debug_assertions)]` real impl / `#[cfg(not(debug_assertions))]` no-op stub — all ~20 call sites compile unchanged; release builds write no `debug_log.txt` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added #[cfg(debug_assertions)] to static LOG_PATH and fn get_log_path() (only referenced from log_to_file debug impl); replaced single log_to_file() with a debug-only real impl and a release-only #[inline(always)] no-op stub with identical signature. All ~20 call sites in lib.rs, layout_utils.rs, and exports.rs compile unchanged in both configurations. cargo check --release -p cxxqt_bridge has zero new warnings (pre-existing build_tiled_pdf_tile_doc warning unrelated). Added 3 tests in dg1_log_gate_tests module: debug_log_to_file_does_not_panic, debug_log_to_file_repeated_calls_are_safe (both #[cfg(debug_assertions)]), release_log_to_file_is_noop_and_does_not_panic (#[cfg(not(debug_assertions))]). cargo test -p cxxqt_bridge: 74 passed, 0 failed.`

## 2026-06-29 (continued, DG.2)

- [X] DG.2 Gate `save_debug_dom()` and `get_out_dir()` in `crates/cxxqt_bridge/src/lib.rs` with dual `#[cfg(debug_assertions)]` / no-op stubs — all 10+ call sites in `lib.rs` and `layout_utils.rs` compile unchanged; release builds write no SVG snapshots and skip `output/` dir creation — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added #[cfg(debug_assertions)] to AtomicUsize/Ordering import and static ADJUST_DOM_COUNTER (only referenced from save_debug_dom debug impl). Replaced single get_out_dir() with a debug-only real impl (creates exe_dir/output/ on first access) and a release-only #[inline(always)] no-op stub that returns OUT_DIR initialized to PathBuf::new() — no directory created. Replaced single save_debug_dom() with a debug-only real impl (increments counter, writes numbered SVG to output/) and a release-only #[inline(always)] no-op stub with identical signature. All ~4 call sites in lib.rs (save_adjust_dom, enter_adjust_mode, accept_adjustments) and ~9 call sites in layout_utils.rs (do_initialize_layout, do_process_layout) compile unchanged. Added 5 tests in dg2_debug_dom_gate_tests module: debug_save_debug_dom_does_not_panic, debug_get_out_dir_returns_output_path, debug_save_debug_dom_repeated_calls_are_safe (all #[cfg(debug_assertions)]), release_save_debug_dom_is_noop_and_does_not_panic, release_get_out_dir_returns_empty_path_no_dir_created (both #[cfg(not(debug_assertions))]). cargo test -p cxxqt_bridge: 77 passed, 0 failed.`

## 2026-06-29 (continued, DG.3)

- [X] DG.3 Gate `cleanup_adjust_output_artifacts()` in `crates/cxxqt_bridge/src/lib.rs` with `#[cfg(debug_assertions)]` / no-op stub — in release no debug files are written so cleanup is correct as a no-op; call sites in `discard_adjustments()` and `exit_adjust_mode()` unchanged — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added #[cfg(debug_assertions)] to the existing cleanup_adjust_output_artifacts() real impl (reads output/ dir, removes adjust_overlay_*.json and adjust_dom_*.svg files, logs removed count). Added #[cfg(not(debug_assertions))] #[inline(always)] no-op stub with identical signature returning 0 — no directory scan or file removal occurs in release builds. Both call sites (discard_adjustments, exit_adjust_mode) compile unchanged in both configurations. Added 3 tests in new dg3_cleanup_gate_tests module: debug_cleanup_adjust_output_artifacts_does_not_panic (returns 0 in test sandbox — no adjust artifacts present), debug_cleanup_adjust_output_artifacts_repeated_calls_are_safe (both #[cfg(debug_assertions)]), release_cleanup_adjust_output_artifacts_is_noop_and_does_not_panic (asserts return == 0, #[cfg(not(debug_assertions))]). cargo test -p cxxqt_bridge: 79 passed, 0 failed.`

## 2026-06-29 (continued, proposed fixes PF3.1–PF3.5)

- [X] PF3.1 Remove `assert_eq!(removed, 0)` from `debug_cleanup_adjust_output_artifacts_does_not_panic` in `crates/cxxqt_bridge/src/lib.rs` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Replaced `let removed = cleanup_adjust_output_artifacts(); assert_eq!(removed, 0, ...)` with `let _ = cleanup_adjust_output_artifacts()` and a comment explaining why — prior test runs may leave adjust artifacts in output/ making the count legitimately non-zero; the test's intent is panic-safety not count verification.`
- [X] PF3.2 Fix release `get_out_dir()` stub in `crates/cxxqt_bridge/src/lib.rs` to return `get_exe_dir().join("output")` instead of `PathBuf::new()` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Changed `OUT_DIR.get_or_init(std::path::PathBuf::new)` to `OUT_DIR.get_or_init(|| get_exe_dir().join("output"))`. Empty PathBuf caused downstream `.join()` calls to produce relative paths (e.g. "adjust_dom.svg") that could write to the current working directory; returning <exe_dir>/output without creating the directory is the correct sentinel — all writes fail gracefully with Err since the directory does not exist. Updated comment to reflect new behavior.`
- [X] PF3.3 Update `release_get_out_dir_returns_empty_path_no_dir_created` (dg2), `release_get_out_dir_returns_empty_path_no_output_dir_created` (dg5), and `release_save_debug_dom_and_log_to_file_produce_no_filesystem_artifacts` (dg5) in `crates/cxxqt_bridge/src/lib.rs` to assert `<exe_dir>/output` path and no-directory-creation — `Status:Done | Priority:P1 | BlockedBy:PF3.2 | CompletedBy:Claude | Date:2026-06-29 | Notes:All three release tests formerly asserted `path.as_os_str().is_empty()`; updated to capture `existed_before = expected.exists()`, call `get_out_dir()`, assert `path == get_exe_dir().join("output")`, then assert `existed_before == expected.exists()` (no dir was created). Added `get_exe_dir` to the import lists in both dg2 and dg5 test modules. Updated DG5 inline comment from "get_out_dir is empty" to "returns <exe_dir>/output without creating it".`
- [X] PF3.4 Normalize mixed CRLF/LF line endings in `crates/cxxqt_bridge/src/lib.rs` to pure LF — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Used PowerShell `[System.IO.File]::ReadAllText` + `.Replace("\r\n", "\n").Replace("\r", "\n")` + `WriteAllText` to convert all 2434 stray CR bytes to pure LF. Verified 0 CR bytes after normalization. Prevents `\` artifacts in grep output and noisy diffs.`
- [X] PF3.5 Fix `E.6` task marker in `docs/status-docs/TODO_SEAMLYLAYOUT_2.md` from `* [ ]` to `- [ ]` to match documented task format — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Changed the E.6 line from `* [ ] E.6 Implement 3D export...` to `- [ ] E.6 Implement 3D export...` (also removed a trailing double-space). Keeps the file self-consistent with the documented `- [ ] Task text` format in the Tag Schema section.`

## 2026-06-29 (continued, proposed fixes PF2.1–PF2.3)

- [X] PF2.1 Fix misleading "empty scene" wording in DG.4 header comment block in `qt_frontend/tests/adjust/AdjustSceneTests.cpp` — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Replaced "an empty scene produces 0 moved pieces so only the JSON file is written (no qWarning)" with "in this test no pieces are moved, so only the JSON file is written (and no qWarning is expected)." The scene loads two pieces (pieceA/pieceB); the real reason no per-piece move logs appear is that no pieces are moved.`
- [X] PF2.2 Add `QFile::remove` cleanup loop after `dumpOverlayData()` in `dg4_debugDumpOverlayDataCompilesAndIsCallable` — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added loop removing adjust_overlay_0.json through adjust_overlay_4.json under applicationDirPath()/output/ immediately after the dumpOverlayData() call in the #ifdef QT_DEBUG block, making the test hermetic and preventing persistent build-directory artifacts.`
- [X] PF2.3 Strengthen `dg4_releaseStubCompilesAndIsCallable`: pre-clean before calling stub; add `QVERIFY2(!QFile::exists(...))` in the `#else` (debug) block — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:In #ifndef QT_DEBUG (release) block: added QFile::remove pre-clean loop before dumpOverlayData() so the no-I/O assertion starts from a known-clean state. In #else (debug) block: added QVERIFY2(!QFile::exists(applicationDirPath()+"/output/adjust_overlay_0.json"), "Release stub must not create overlay dump files") before the QSKIP, asserting no dump file survived from prior test setup. Together these turn the test from "callable without crashing" into "callable without creating files."`

## 2026-06-29 (continued, proposed fixes PF.1–PF.5)

- [X] PF.1 Remove stale per-file occurrence counts from DG.5 header comment in `crates/cxxqt_bridge/src/lib.rs` — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Removed ~141/~27/~26 call-site counts from the three-bullet file list in the DG.5 comment block; counts drift with every refactor and were already stale.`
- [X] PF.2 Replace hard-coded `debug.txt` filename with `debug log file` in DG.1 line of DG.5 header comment in `crates/cxxqt_bridge/src/lib.rs` — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Changed "(debug.txt writes; all pipelines)" to "(debug log file writes; all pipelines)"; actual default path is output/debug_log.txt and is overridable via SEAMLY_LOG_FILE.`
- [X] PF.3 Replace `no debug.txt is written` with `no debug log file is written` in DG.5 release acceptance contract comment in `crates/cxxqt_bridge/src/lib.rs` — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Same filename inaccuracy as PF.2; updated acceptance contract wording to be filename-agnostic.`
- [X] PF.4 Correct `MIGRATION_STATUS.md` row 8c: `dg5_verification_tests` count 6→5; call-site counts lib.rs ~141→~123, layout_utils.rs ~26→~24 — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Updated the 8c Completed row to match the actual module (5 tests) and current gated-call occurrence counts.`
- [X] PF.5 Correct `project-docs/TODO_COMPLETED.md` DG.5 entry: same test count (6→5) and call-site count corrections (lib.rs ~141→~123, layout_utils.rs ~26→~24) — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Updated the original DG.5 completion log entry to match actual implementation.`

## 2026-06-29 (continued, DG.5)

- [X] DG.5 Verify debug gate correctness: `cargo build --release` leaves no `output/` artifacts; `cargo test -p cxxqt_bridge` green in both debug and release configs; update `DECISIONS.md` Decision-002 scope from AdjustMode to all observability file I/O app-wide — `Status:Done | Priority:P1 | BlockedBy:DG.1,DG.2,DG.3,DG.4 | CompletedBy:Claude | Date:2026-06-29 | Notes:Added dg5_verification_tests module in crates/cxxqt_bridge/src/lib.rs with 5 tests spanning both build configurations: debug_all_rust_observability_gates_compile_and_run_without_panic (calls log_to_file + save_debug_dom + get_out_dir + cleanup_adjust_output_artifacts in sequence; asserts get_out_dir last component is "output"), debug_repeated_calls_to_all_observability_gates_are_safe (3 iterations of all three functions), release_all_rust_observability_gates_are_noops_and_do_not_panic (combined no-op check; asserts cleanup returns 0), release_get_out_dir_returns_empty_path_no_output_dir_created (asserts empty PathBuf — no output/ dir ever created), release_save_debug_dom_and_log_to_file_produce_no_filesystem_artifacts (pipeline-order call sequence; asserts get_out_dir empty). cargo test -p cxxqt_bridge: 81 passed, 0 failed (debug). cargo test --release -p cxxqt_bridge: 79 passed, 0 failed (release). cargo build --release -p cxxqt_bridge: Finished — no output/ directory created (verified Test-Path false). Decision-002 in DECISIONS.md updated: title changed to "All observability file I/O gated app-wide (compile-time)"; added Scope section listing DG.1–DG.4 gates with call-site counts (lib.rs ~123, exports.rs ~27, layout_utils.rs ~24); added Verification section; Decision deadline updated to 2026-06-29. Change Log entry added.`

## 2026-06-29 (continued, DG.4)

- [X] DG.4 Gate `dumpOverlayData()` declaration in `qt_frontend/src/adjust/AdjustScene.h` and definition in `AdjustScene.cpp` with `#ifdef QT_DEBUG` / `#else` empty-body stub — three call sites in `AdjustWindow.cpp` compile unchanged — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Gated the dumpOverlayData() declaration in AdjustScene.h: #ifdef QT_DEBUG real declaration / #else inline void dumpOverlayData() const {} empty stub. Gated the static int s_overlayDumpCounter member declaration in AdjustScene.h with #ifdef QT_DEBUG / #endif (only used in the debug-only body). Gated the static member definition (int AdjustScene::s_overlayDumpCounter = 0) and the full function body in AdjustScene.cpp with #ifdef QT_DEBUG / #endif QT_DEBUG. Three call sites in AdjustWindow.cpp (after initial loadLayout, before onApplyClicked apply, after reload loadLayout) compile unchanged in both debug and release — debug resolves to the real implementation, release resolves to the inline no-op. Added 2 Qt tests to AdjustSceneTests.cpp: dg4_debugDumpOverlayDataCompilesAndIsCallable (#ifdef QT_DEBUG path: calls scene.dumpOverlayData() and asserts no crash; QSKIP in release) and dg4_releaseStubCompilesAndIsCallable (#ifndef QT_DEBUG path: calls stub and asserts no crash; QSKIP in debug). AdjustSceneTests: 22 passed, 0 failed, 1 skipped (release-only test correctly skipped in debug build).`

## 2026-06-29

- [X] BE.1 Emit `progress_updated(percent)` during DXF export — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added &mut impl FnMut(i32) progress callback to do_export_dxf() in crates/cxxqt_bridge/src/exports.rs. Three intermediate ticks emitted in order: 10% (SVG→ezdxf conversion starting), 50% (ezdxf Drawing ready / DXF file write starting), 90% (DXF file written). Caller (export_dxf in lib.rs) retains ownership of 0% (before call) and 100% (after Ok return). Progress closure in lib.rs export_dxf() calls set_export_progress(pct) + progress_updated(pct) so the QML ProgressBar binding (appController.exportProgress) and any imperative signal listeners both receive the intermediate values. Updated 3 existing test calls to pass no-op &mut |_| {}. Added new test do_export_dxf_emits_intermediate_progress: asserts exactly 3 ticks, each in 1–99, in non-decreasing order, with pinned values [10, 50, 90]. Full cxxqt_bridge suite: 70 passed, 0 failed.`
- [X] BE.2 PDF export implementation (dialog + bridge + renderer path) — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added &mut impl FnMut(i32) progress callback to do_export_pdf() in crates/cxxqt_bridge/src/exports.rs. Two intermediate ticks: 10% (SVG DOM → usvg tree parse starting) and 90% (PDF file written to disk). Caller (export_pdf in lib.rs) retains ownership of 0% (before call) and 100% (after Ok return). For the single-page path, a closure in lib.rs export_pdf() calls set_export_progress(pct) + progress_updated(pct) so the QML ProgressBar (appController.exportProgress binding) receives both intermediate values. For the sheet-mode path (do_export_sheets_pdf), lib.rs now manually emits 10% before the call and 90% after Ok, giving the same visual feedback without changing the sheets.rs function signature. Updated 2 existing test calls to pass no-op &mut |_| {}. Added new test do_export_pdf_emits_intermediate_progress: asserts exactly 2 ticks, each in 1–99, in non-decreasing order, with pinned values [10, 90]. Full cxxqt_bridge suite: 71 passed, 0 failed.`
- [X] BE.3 PNG export progress callback in bridge — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added &mut impl FnMut(i32) progress callback to do_export_png() in crates/cxxqt_bridge/src/exports.rs. Two intermediate ticks: 10% (SVG DOM → usvg tree parse starting) and 90% (PNG file written to disk). Caller (export_png in lib.rs) retains ownership of 0% (before call) and 100% (after Ok return). A closure in lib.rs export_png() calls set_export_progress(pct) + progress_updated(pct) so the QML ProgressBar (appController.exportProgress binding) receives both intermediate values. Updated 2 existing test calls to pass no-op &mut |_| {}. Added new test do_export_png_emits_intermediate_progress: asserts exactly 2 ticks, each in 1–99, in non-decreasing order, with pinned values [10, 90]. Full cxxqt_bridge suite: 72 passed, 0 failed.`
- [X] BC.2 Verify settings round-trip: load file → fields update → save → JSON matches — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-29 | Notes:Added 4 SettingsModelTests in qt_frontend/tests/settings/SettingsModelTests.cpp: (1) bc2_loadFile_allFieldsUpdate — loads a fixture with all 21 loadable fields set to non-default values and asserts every model property reflects the fixture; (2) bc2_saveAfterLoad_savedJsonContainsAllKeys — loads a file, saves, and asserts all 22 expected JSON keys (including outputFormat) are present in the saved document; (3) bc2_savedJson_fieldValuesMatchModel — loads a complete fixture, saves, then compares every JSON value against the matching model getter to catch precision loss or key mismatches; (4) bc2_fullPipeline_loadEditSaveReload_fieldsMatch — full workflow: loads file → edits 9 fields via setters (simulating dialog interaction) → saves → reloads into a fresh model → verifies all edited fields survived the round-trip. All 24 SettingsModelTests green. No production code changes required — round-trip is correct.`
- [X] DQ.1 AdjustMode file-I/O elimination completion scope and verification gate — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:slspencer | Date:2026-06-29 | Notes:Decision-002 accepted as Option C (compile-time gate): #[cfg(debug_assertions)] in Rust / #ifdef QT_DEBUG in C++ allows AdjustMode disk writes in debug builds; release builds enforce strict no-disk-I/O. Rationale: preserves debuggability without shipping I/O to release; compile-time enforcement is deterministic and requires no runtime flag management. DECISIONS.md updated to Status:Accepted.`

## 2026-06-30 (proposed fixes PF4.1–PF4.6)

- [X] PF4.1 Add `QT_QPA_PLATFORM=offscreen` to `set_tests_properties` for `AdjustSceneTests` and `AdjustControllerTests` in `CMakeLists.txt` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:Added set_tests_properties(AdjustSceneTests AdjustControllerTests PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen") after the add_test(NAME AdjustControllerTests) line in qt_frontend/CMakeLists.txt. Both QtWidgets test executables will now run with the offscreen platform plugin in headless CI environments, preventing failures caused by top-level window creation without a display.`
- [X] PF4.2 Add `QTest::qWait(0)` after `closeAdjustWindow()` in `launchWindowWithValidSvgDoesNotCrash` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:Added QTest::qWait(0) immediately after controller.closeAdjustWindow() in launchWindowWithValidSvgDoesNotCrash() in qt_frontend/tests/adjust/AdjustControllerTests.cpp. Drains the event loop so the WA_DeleteOnClose AdjustWindow is actually deleted before the test function returns, preventing stray top-level windows and flaky behavior between tests in headless runs.`
- [X] PF4.3 Add `QTest::qWait(0)` after `closeAdjustWindow()` in `launchWindowTwiceDoesNotCrash` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:Same fix as PF4.2 applied to launchWindowTwiceDoesNotCrash() in qt_frontend/tests/adjust/AdjustControllerTests.cpp. Ensures the WA_DeleteOnClose window is destroyed before the test returns, reducing resource leakage between tests.`
- [X] PF4.4 Add explicit `QVERIFY2(obj.contains(key), ...)` key-presence loop in `bc2_savedJson_fieldValuesMatchModel` — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:Added a QStringList expectedKeys with all 22 keys and a for-loop calling QVERIFY2(obj.contains(key), qPrintable("Missing key in saved JSON: " + key)) before the value QCOMPARE block in bc2_savedJson_fieldValuesMatchModel() in qt_frontend/tests/settings/SettingsModelTests.cpp. Prevents false positives when a missing key's absence is masked because its absent JSON value coerces to 0/false/"" matching a model getter that also returns a default.`
- [X] PF4.5 Replace stale "split out the flag bytes" comment in `lib.rs export_pdf` closure block — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:Replaced the 3-line "Build a progress closure / The closure borrows self mutably; split out the flag bytes / double-borrow" comment block with a 3-line accurate summary: "Progress handling: emit 0%/100% here; forward intermediate ticks from the export path / (single-page via a progress closure, sheet-mode via manual 10%/90% ticks). / Keep self borrows scoped so the closure can borrow self mutably when needed." The old comment described a refactor step that no longer applies and would mislead future refactors.`
- [X] PF4.6 Fix "Stage 2 complete" comment in `exports.rs do_export_pdf` to "Stage 3 complete" — `Status:Done | Priority:P3 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:Changed "// Stage 2 complete: PDF file written." to "// Stage 3 complete: PDF file written." at line 729 of crates/cxxqt_bridge/src/exports.rs inside do_export_pdf(). The pipeline comment above this block describes three stages; at this point in the function all three stages are complete, so the stage counter was off by one.`

## 2026-06-30 (Adjust rotation pivot: center, not corner)

- [X] In Adjust Mode, the piece rotation center point should not be the upper left corner of the piece's bounding box (this is easy to code and is the current behavior). The piece rotation center point should be the center point of the piece's bounding box (this is more user friendly but more difficult to implement in code). — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-30 | Notes:qt_frontend/src/adjust/PieceOverlayItem.cpp: setTransformOriginPoint(0,0) changed to setTransformOriginPoint(m_w/2.0, m_h/2.0) in the constructor and contextMenuEvent() (was upper-left bbox corner, now bbox center); buildTransform() rewritten to build the SVG matrix as a rotate-about-center operation (translate pivot from base-position+half-extent to current-position+half-extent, rotate, scale/flip about the same center, translate back) so the persisted transform_str matches the new QGraphicsItem pivot; applyTransformString() rewritten to map the bbox-center point (not the local origin) through the cumulative SVG transform to recover rotation angle and pos(), keeping overlay/SVG alignment across loadLayout()/Undo/Redo round-trips. 4 new AdjustSceneTests added: rotationPivotIsBoundingBoxCenter (transformOriginPoint() == bbox center), rotatingAboutCenterKeepsCenterFixed (sceneBoundingRect().center() unchanged after setRotation()), buildTransformRoundTripsRotationAroundCenter and buildTransformRoundTripsMoveAndRotationAroundCenter (buildTransform() output replayed through the constructor's applyTransformString() path reproduces the same pos()/rotation()). Full AdjustSceneTests suite (26 tests, including the 4 new ones) passes with zero regressions: built via cmake --build --preset debug --target AdjustSceneTests, run via AdjustSceneTests.exe with QT_QPA_PLATFORM=offscreen — Totals: 26 passed, 0 failed.`

## 2026-06-26

- [X] L.2 Implement paper_type="sheets" in stages — `Status:Done | Priority:P2 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-26 | Notes:All three sub-tasks completed: L.2.1 Phase A (tiled PDF pages for oversized pieces, Date:2026-06-22), L.2.2 Phase B (remaining-piece MaxRects packing onto one-or-more sheet SVGs with merged PDF output, Date:2026-06-23), L.2.3 (clip off-page labels in tiled PDF pages via explicit clipPath + overflow=hidden, Date:2026-06-25). Full cxxqt_bridge suite green at 51 passed after viewbox_mm_scaling regression fix. Parent task removed from TODO.md.`
- [X] BC.1 Phase 5 bug: `SettingsModel::load()` must emit property notify signals so dialog fields refresh after load-from-file — `Status:Done | Priority:P1 | BlockedBy:none | CompletedBy:Claude | Date:2026-06-26 | Notes:Root cause: setters suppress emission when value is unchanged (qFuzzyCompare / == guard), so for values that match the current model state no signal fires and QML controls with runtime-broken bindings (RadioButton.checked, ComboBox.currentIndex, TextField.text broken by prior user interaction) are never refreshed. Fix: (1) Added void settingsLoaded() signal to SettingsModel.h — a semantic batch-complete signal emitted unconditionally at end of load(). (2) Added force-emit block in SettingsModel.cpp load() that emits every Q_PROPERTY NOTIFY signal plus settingsLoaded() before returning true. (3) Fixed SettingsDialog.qml: gave IDs to all interactive controls (paperTypeCombo, paperSizeCombo, tileSizeCombo, tileOrientationCombo, rollSizeCombo, fabricWidthField, selvedgeWidthField, marginTopField, marginBottomField, marginLeftField, marginRightField) and added Connections { onSettingsLoaded } block that explicitly resets checked/currentIndex/text for every radio button, checkbox, combobox, and text field. (4) Added 5 QSignalSpy tests in SettingsModelTests.cpp (load_emitsSettingsLoadedSignal, load_emitsUnitChangedEvenWhenValueUnchanged, load_emitsMarginSignalsEvenWhenValuesUnchanged, load_emitsLayoutModeChangedEvenWhenValueUnchanged, load_emitsAllPropertySignals) — all pass. SettingsModelTests 100% green.`
