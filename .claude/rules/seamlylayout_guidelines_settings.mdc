# Settings Guidelines

## Reference

See full documentation in [`settings-docs/SETTINGS_WORKFLOW.md`](../../project-docs/seamlylayout-docs/settings-docs/seamlylayout_SETTINGS_WORKFLOW.md)

## Default Units

| Unit | Margin Default |
|------|----------------|
| `in` | 0.25 |
| `cm` | 1 |
| `mm` | 10 |

## Media Types

| Media | Sub-type | Default |
|-------|----------|---------|
| `paper` | `sheet` | Arch E (36×48 in) |
| `paper` | `roll` | 36 in width |
| `paper` | `tiled` | Letter (8.5×11 in) |
| `fabric` | — | User-defined dimensions |

## Key Constraints

- Settings file extension: `.settings`
- Settings directory: `/settings`
- Paper sizes source: `/assets/paperSizes/paper_sizes`
- Application preferences file: `QStandardPaths::AppConfigLocation/qt6_seamlylayout.ini`
- Default preferences profile: `default_preferences.json`

## Piece Arrangement Options

`layoutMode` (radio group) — all modes preprocess the piece so the grainline points up.

| Mode | Description | Trial set |
|------|-------------|-----------|
| `alongGrainline` | Default. Grain-up baseline; allows head-up or head-down. | `{0°, 180°}` |
| `withNap` | Strict no-flip — every piece points the same direction (napped or directional fabric). | `{rotationStep°}` (singleton) |
| `any` | Grainline ignored; packer picks the best quarter turn per piece. | `{0°, 90°, 180°, 270°}` |

`rotationStep` semantics depend on `layoutMode`:

| Mode | Sub-radio label | Allowed `rotationStep` | Default |
|------|----------------|------------------------|---------|
| `alongGrainline` | _(no sub-radio)_ | unused | — |
| `withNap` | **Nap Direction** — Pieces point Up / Pieces point Down | `{ 0, 180 }` | `0` (head-up) |
| `any` | _(no sub-radio)_ | unused | — |

`noGrainlineRotation` (radio group, hidden in `any` mode) — pieces with no grain direction.

| Value | Label | Behavior |
|-------|-------|----------|
| `upright` | Keep upright | Default. Drafted orientation; mode trial set only. |
| `free` | Rotate freely | Packer may also try 90° and 270° for these pieces. |

Grain direction sources, in order: drawn `grainline` group, then `data-grainline-angle`. No guess from the outline.

## Viewer Preferences

- DXF/PDF viewers: Use OS default if path empty
- 3D viewer: Default URL is `https://3mfviewer.com/`
